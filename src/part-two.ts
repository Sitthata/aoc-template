import readInput from "./input/read-file";

const dataArray: string[] = readInput("data.txt");
const testArray: string[] = [];

const partTwoSolution = (dataArray: string[]): number => {
    return 0;
}

console.log(`Part one answer is ${partTwoSolution(testArray)}`)